import (c) 'stdio.h'
import (c) 'string.h'

var __stdinp &FILE external
stdin => __stdinp

type String = &[byte:]

char(s String) => s[0]

main {
    var buffer [byte:100],

    buffer.fgets(100, stdin)
    buffer[buffer.strlen - 1] = '\0'.char

    buffer.strcat(' World!')
    buffer.puts
}
