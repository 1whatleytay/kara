import 'print.kara'

type Pizza {
    name &string
    flavor int
}

make *Pizza => *Pizza('hey', 8) // no delete here, there should be a double free... whatever

main int {
    let a = Pizza('wow', 4)
    let b = make

    a.name.puts
    b.name.puts

    return 0
}
