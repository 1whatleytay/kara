import (c) 'stdio.h'
// import (c) 'stdlib.h'

itoa(value int, buffer &[byte:], size int) nothing external

main int {
    var buffer [byte:100]
    var z = 4

    for {
        if z < 11 {
            z += 1
            continue
        } else if z > 40 {
            break
        }

        z.itoa(buffer, 10)
        buffer.puts

        z += 2
    }

    return 0
}